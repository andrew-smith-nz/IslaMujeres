
import Reactotron from 'reactotron-react-native';

const gallery = [
    // Meson del Bucanero
    { id: "cedf40e4be92a0a69e1bc943868c054e", resource: { uri: 'lola'}, sortOrder: 0 }
]

    export function getPhotos(id){
        // return gallery.filter(photo => photo.id === id);
        // TODO: Add more photos
        return gallery;
    }
